langcode: en
status: true
dependencies:
  module:
    - menu_ui
third_party_settings:
  menu_ui:
    available_menus:
      - main
    parent: 'main:'
name: 'Landing Page'
type: landing_page
description: "A <em>Landing Page</em> is used for aggregating other pieces of content on the site, and used as a jumping off point; it doesn't/shouldn't hold much unique content itself."
help: null
new_revision: true
preview_mode: 1
display_submitted: false
